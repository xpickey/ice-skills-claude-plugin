# runtime artifacts — never commit (platform-specific, large, rebuildable)
.venv/
**/data/browser_state/
**/__pycache__/
*.pyc
node_modules/
**/*.tflite
.DS_Store

# backup files — never commit
*.bak
*.bak.*
**/*.bak
agt-skill-pack/
.venv-doc/
**/.venv-doc/

# runtime/log/cache ที่มากับ active skills — ไม่ commit
*.log
**/assets/templates-cache/
**/evals/
**/thesis-ai-det-col/archive/

# ไฟล์ลายนิ้วมือของตัวบันทึกหลัง build (สร้างอัตโนมัติ ไม่ต้องเก็บเข้าคลัง)
.last-built.json
