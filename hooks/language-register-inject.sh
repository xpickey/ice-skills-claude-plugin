#!/bin/bash
# ยัดกติกาภาษากลางเข้า context ของทุก session โดยอัตโนมัติ (คำสั่ง user 2026.08.16:
# กติกาต้องถูกอ่านและบังคับใช้จากไฟล์กลางไฟล์เดียว ห้ามทำสำเนาในไฟล์ agent)
# อ่านสดจากไฟล์จริงทุกครั้ง — แก้ไฟล์กลางแล้วมีผลกับ session ใหม่ทันที ไม่มีสำเนาให้ตกรุ่น
F="$HOME/.claude/agents/reference/language-register.md"
if [ -f "$F" ]; then
  echo "=== กติกาภาษากลางของทีม (บังคับใช้กับทุกข้อความถึง user และทุกเอกสาร — โหลดอัตโนมัติโดยระบบ ==="
  cat "$F"
  echo "=== จบกติกาภาษากลาง — ทุกข้อข้างบนเป็นข้อบังคับ ไม่ใช่คำแนะนำ ==="
fi
